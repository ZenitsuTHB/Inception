
version: '3'
services:
	wordpress:
	image: wordpress:latest
	volumes:
		-wp_data:/var/www/html
	ports:
		-"80:80"
	environment:
		WORDPRESS_DB_USER:
		WORDPRESS_DB_PASSWORD:
		WORDPRESS_DB_HOST:
	db:
		image: maria:latest
		volumes:
			
volumes:
	wp_data
	db_data
